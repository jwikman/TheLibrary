{
    "name": "NAB ruleset",
    "includedRuleSets": [
        {
            "action": "Default",
            "path": "./LinterCop.ruleset.json"
        }
    ],
    "rules": [
        {
            "id": "AA0219",
            "action": "Warning",
            "justification": "The Tooltip property of Fields should start with 'Specifies'."
        },
        {
            "id": "AA0215",
            "action": "None",
            "justification": "File names should not include the object prefix (LIB) as per project coding standards."
        }
    ]
}
